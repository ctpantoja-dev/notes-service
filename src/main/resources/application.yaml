spring:
    application:
        name: notes-service
    datasource:
        url: jdbc:h2:file:/data/notes-db
        username: root
        password: admin123
        driverClassName: org.h2.Driver
    jpa:
        database-platform: org.hibernate.dialect.H2Dialect
        hibernate:
            ddl-auto: create
    h2:
        console:
            enabled: true
